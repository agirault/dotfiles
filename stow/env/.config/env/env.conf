# Shared environment configuration
# Parsed by env_loader.fish (fish) and 00-env-loader.sh (bash)
# Sections: [prepend] KEY=value, [export] KEY=value, [alias] name=command

[prepend]
PATH=/usr/local/cuda/bin
PATH=$HOME/.local/bin

[export]
GPG_TTY=$(tty)

[alias]
c=cursor-agent
